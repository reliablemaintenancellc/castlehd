name: Find My Code
on: [push, workflow_dispatch]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: 🕵️ List All Files
        run: |
          echo "=========================================="
          echo "      HERE IS THE ACTUAL FILE STRUCTURE   "
          echo "=========================================="
          find . -maxdepth 2 -not -path '*/.*'
          echo "=========================================="
